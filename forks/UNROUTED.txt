# Fork call sites that still reach the host.
#
# `cargo xtask check lint` lints every fork for `clippy::disallowed_methods`
# after the workspace, and compares what it finds against this file. A site that
# is not listed fails the gate; a listed site that no longer fires fails it too,
# per D13's mirror rule -- otherwise the list accumulates entries that describe
# nothing and stops measuring.
#
# One line per (file, banned method) with a count. Line and column are
# deliberately absent: they move for reasons that have nothing to do with
# confinement, and a key that churns is a key nobody maintains.
#
# **Every line below is deliberate.** The backlog this file was created to hold
# -- 133 calls that asked the host what they should have asked the namespace --
# is empty. That is the state to keep it in: a new entry here should be argued
# for in the same terms as the ones already listed, not added because a lint
# went red.
#
# ---------------------------------------------------------------------------
# Build scripts run at build time on the host, before any namespace exists, and
# cannot see `brush_vfs` at all. Routing `sed/build.rs` simply did not compile,
# which is the honest answer.
#
# `uu_df` reads the *host mount table*: `filesystem.rs` canonicalizes mount
# device names and `df.rs` resolves those same names. `/dev/disk1s1` is host
# introspection, not namespace access, and routing it would make the symlink
# resolution never fire. `df` is not confined, by decision; `uucore`'s `fsext`
# is the same class. See RESIDUAL-PATCHES.md.
#
# `uucore::perms` is deliberately left on `walkdir`: unreachable from anything
# bundled, and porting it needs a routed `chown` that does not exist.
#
# The `rustix` `*at` calls in `uu_mv` and `uucore::safe_traversal` sit below a
# descriptor that was already rooted through the namespace and inherit its
# confinement. Rooting is the patch; the `*at` calls under it are meant to be
# unchanged.
#
# `mkfifo`, `mknod`, `utimensat`, `UnixListener::bind` and a child's
# `Command::current_dir` have no facade equivalent. The ban list says "not
# expressible in the namespace yet" for each, and means it. Each is a real hole
# for the day something needs it, recorded rather than papered over: a FIFO or
# device node created through `cp -a` or `mv` across mounts lands on the host,
# and `find -exec` runs its child in the host's working directory.
#
# `tempfile` and `env::temp_dir` are scratch space, which D38 places outside
# the namespace on purpose.
# ---------------------------------------------------------------------------

findutils/src/find/matchers/exec.rs std::process::Command::current_dir 2
sed/build.rs std::fs::File::create 1
uu_cp/src/cp.rs nix::sys::stat::mknod 1
uu_cp/src/cp.rs nix::unistd::mkfifo 1
uu_cp/src/cp.rs std::os::unix::net::UnixListener::bind 1
uu_df/src/df.rs std::path::Path::is_symlink 1
uu_df/src/filesystem.rs std::fs::canonicalize 1
uu_df/src/filesystem.rs std::path::Path::canonicalize 3
uu_df/src/filesystem.rs std::path::Path::metadata 2
uu_mv/src/mv.rs nix::unistd::mkfifo 2
uu_mv/src/mv.rs rustix::fs::renameat 1
uu_mv/src/mv.rs rustix::fs::symlinkat 1
uu_mv/src/mv.rs rustix::fs::unlinkat 1
uu_sort/src/sort.rs std::env::temp_dir 1
uu_sort/src/tmp_dir.rs tempfile::Builder::tempdir_in 1
uu_tac/src/tac.rs tempfile::tempfile 1
uu_touch/src/touch.rs rustix::fs::utimensat 1
uucore/build.rs std::fs::File::create 1
uucore/build.rs std::fs::read_dir 3
uucore/build.rs std::fs::read_to_string 3
uucore/build.rs std::path::Path::exists 3
uucore/build.rs std::path::Path::is_dir 2
uucore/build.rs std::path::Path::is_file 1
